{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

        {% if user.roles|length > 1 %}


        {% endif %}
        {{ form_row(form.roles) }}
        <button type="submit" class="btn btn-primary" formnovalidate>
            Submit Changes
        </button>
        <a href="{{ path('list_users') }}" class="btn btn-danger" role="button">
            Cancel
        </a>
        {{ form_end(form) }}
    </div>
{% endblock %}

